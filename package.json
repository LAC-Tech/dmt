{
  "name": "@lewis-campbell/dmt",
  "version": "1.2.0",
  "description": "A declarative, minimalist testing library for the browser",
  "main": "dist.js",
  "types": "src/types.d.ts",
  "type": "module",
  "dependencies": {
    "diff": "^5.0.0",
    "rambda": "^7.0.1"
  },
  "devDependencies": {
    "@parcel/packager-ts": "^2.3.2",
    "@parcel/transformer-typescript-types": "^2.3.2",
    "@types/diff": "^5.0.1",
    "@types/node": "^16.11.10",
    "esbuild": "^0.14.23",
    "os-browserify": "^0.3.0",
    "parcel": "^2.2.1",
    "typescript": "^4.5.5"
  },
  "scripts": {
    "check": "tsc --watch",
    "test": "parcel test/index.html",
    "build": "esbuild  src/index.js --minify --bundle --format=esm --outfile=dist.js"
  }
}
